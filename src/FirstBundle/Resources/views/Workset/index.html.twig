{# Workset/Index #}

{% extends "FirstBundle:Layouts:layout.html.twig" %}

{% block title %}
    List Workset
    
{% endblock  %}

{% block content %}
<div class="container">
    <div class="row">
        
        <div class="col-md-12">
            <h2>

                List Workset
            </h2>
        </div>

    </div>
    <div class="row">
        <div class="panel-custom col-md-12">
            
            <div class="table-responsive table">
                
{#                LISTE DES WORKSET (TABLEAU)#}
                
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Generic</th>
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                        
                    <tbody>
                    {% for workset in worksets %}
                        <tr>
                            <td class="col-md-1">
                                {{ workset.id }} - 
                            </td>     
                            <td class="col-md-3">
                                <a href="{{ path('work_workset', {'id': workset.id }) }}">
                                    <b>{{ workset.name }} </b>
                                </a>
                            </td>
                            <td class="col-md-1">
                                {% if workset.generic == 1 %}
                                    Oui
                                {% else %}
                                    Non
                                {% endif %}
                            </td>
                            <td class="col-md-5">
                                {{ workset.description }}
                            </td>
                            <td>
                                <div class="btn-group">

                                    <btn href="{{ path('view_workset', {'id': workset.id }) }}" class="btn btn-ghost-darkblue">
                                        <span class="glyphicon glyphicon-cog"></span>
                                    </btn>
                                    <btn href="{{ path('edit_workset', {'id': workset.id }) }}" class="btn btn-ghost-orange">
                                        <span class="glyphicon glyphicon-pencil"></span>
                                    </btn>
                                    <btn href="{{ path('delete_workset', {'id': workset.id }) }}" class="btn btn-ghost-blood">
                                        <span class="glyphicon glyphicon-trash"></span>
                                    </btn>
                                </div>
                            </td>
              
                        </tr>
                    {% else %}
                    {% endfor %}
                    </tbody>
                
                </table>
            </div>
            <div class="col-md-1 col-md-offset-11">
                <a href="{{ path('create_workset') }}">
                    <button type="button" class="btn btn-ghost-green">
                        <span class="glyphicon glyphicon-plus"></span>
                        &nbsp; Nouveau
                    </button>
                </a>
            </div>
        </div>
    </div>
 </div>
    
{% endblock  %}
